# Optional. Without DATABASE_URL the harness uses an in-memory store so
# the dev server still boots. Use db:push once Postgres is attached.
DATABASE_URL=

# Add later through Replit Secrets. Never commit the key.
# The xAI client currently throws "not wired" even if this is set.
# XAI_API_KEY=
