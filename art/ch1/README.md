# Capítulo 1 art

Optional print-grade JPEG: `courtyard.jpg` (~1980×1400 interior courtyard).

GitHub MCP cannot store binary images faithfully. If this file is added later, upload it in the GitHub web UI as NextGreatEra (same path used for the OFL TTFs). `server/lib/print.ts` embeds it when present and always draws live `la maleta` / `Lola` labels. Without the JPEG, chapter 1 ships card chrome + live type only.
